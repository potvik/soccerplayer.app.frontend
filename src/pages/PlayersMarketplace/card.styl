.cardContainer {
  width 250px

  margin-bottom: 50px;

  &:hover {
    transform: scale(1.05)
    transition all 0.2s
    box-shadow: 6px 6px 18px 7px rgba(147,151,172,.19);
  }

  .buyButton {
    // background: #03ade8;
    background: #1c2a5e;
    padding 15px;
    text-align center
    cursor pointer

    &:hover {
      background: #03ade8;
    }
  }

  .infoBlock {
    // background grey
    background: #b4fddf;
  }
}
